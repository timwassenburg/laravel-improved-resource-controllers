<?php

return [
    /*
     * The php artisan make:controller --resource command will use the improved resource controllers by default.
     * You can still generate empty resource controllers by adding the --empty (or -e) param. If you prefer to use empty
     * resource controllers by default change this value to false and add the --crud (or -c) param to generate the
     * improved resource controllers.
     */
    'use_by_default' => true,

    /*
     * The namespace of your models.
     */
    'model_namespace' => 'App\Models\\',
];
